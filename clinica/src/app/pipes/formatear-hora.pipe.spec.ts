import { FormatearHoraPipe } from './formatear-hora.pipe';

describe('FormatearHoraPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatearHoraPipe();
    expect(pipe).toBeTruthy();
  });
});
